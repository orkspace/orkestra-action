# orkestra-action
A versioned GitHub Action for running Orkestra CLI commands in CI. Supports validation, templating, RBAC generation, ConfigMap generation, bundle generation, and init workflows with deterministic outputs.
