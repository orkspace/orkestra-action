# Examples

- `single-operator.yml` — run `ork e2e` for a single operator with `e2e.yaml` at the repo root
- `matrix.yml` — run `ork e2e` across multiple operators in a matrix job

Copy either file into `.github/workflows/` and adjust the `matrix.example` list to match your operator directories.
